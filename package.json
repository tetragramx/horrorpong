{
  "name": "horror-pong",
  "version": "1.0.0",
  "description": "Horror Pong monorepo",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "check": "npm --prefix server run check && npm --prefix client run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
